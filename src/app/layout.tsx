import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Inter_Tight } from "next/font/google";



export const metadata: Metadata = {
  title: 'Sea Edge Resort | Luxury Family Resort in Sharm El Sheikh',
  description: 'Experience luxury Red Sea hospitality at Sea Edge Resort. All-inclusive resort with private beach, aqua park, and world-class dining. Book direct today.',
  openGraph: {
    "title": "Sea Edge Resort | Sharm El Sheikh Luxury Resort",
    "description": "Experience family fun and Red Sea luxury at Sea Edge Resort. All-inclusive beachfront accommodation.",
    "siteName": "Sea Edge Resort",
    "type": "website"
  },
};

const interTight = Inter_Tight({
  variable: "--font-inter-tight",
  subsets: ["latin"],
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${interTight.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
